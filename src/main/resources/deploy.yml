stack: spring
role: library

deploy:
  enabled: false            # library tidak dirun di server

artifact:
  publish: nexus
  base_url: https://nexus.dwiaribowokj.xyz
  repository: maven-snapshots   # pakai maven-snapshots jika versi berakhiran -SNAPSHOT
  groupId: <com.wiee.usercompany>
  artifactId: user-company-core-v1
  version: auto               # ambil dari pom.xml / build.gradle
  packaging: jar
